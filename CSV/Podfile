# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'csvSearch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for csvSearch
pod "SwiftCSV"
  target 'csvSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'csvSearchUITests' do
    # Pods for testing
  end

end
